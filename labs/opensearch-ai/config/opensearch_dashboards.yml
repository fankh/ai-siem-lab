server.name: ai-siem-dashboards
server.host: "0.0.0.0"
opensearch.hosts: ["http://opensearch:9200"]
opensearch.ssl.verificationMode: none

# OpenSearch Assistant(챗) 활성화 — 동작하려면 ml-commons에 root agent 등록 필요(README 참조)
assistant.chat.enabled: true
